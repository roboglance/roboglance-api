[project]
name = "roboglance"
version = "0.1.0"
description = "The backend API of RoboGlance."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard-no-fastapi-cloud-cli]>=0.139.0",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]

[tool.uv]
required-version = ">=0.11.8"

[tool.fastapi]
entrypoint = "app.main:app"
